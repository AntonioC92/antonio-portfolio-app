/resources/insights/* /insights/:splat 301
/privacypolicy /privacy-policy 301

# Added 2026-08-10, fixing GSC page-indexing findings:
# future-fractional-cmo-operating-builder was never a file in
# src/content/resources/ (confirmed via git log --all) and has 404'd
# since Google first indexed it 2026-03-15. Redirecting to the closest
# live equivalent article instead of leaving it a dead end.
# Both forms are needed: Cloudflare _redirects matches literally, and
# Google's crawler was found (via URL Inspection API, same day) to be
# checking the trailing-slash form specifically — the no-slash-only rule
# left that variant still 404ing after the first fix went live.
/insights/future-fractional-cmo-operating-builder /insights/marketing-manager-vs-fractional-cmo 301
/insights/future-fractional-cmo-operating-builder/ /insights/marketing-manager-vs-fractional-cmo 301
# /fractional-cmo previously only had a client-side React Router redirect
# (<Navigate to="/" replace/> in entry-server.tsx / main.tsx), which sends
# no HTTP status to crawlers or non-JS clients. Adding the matching
# server-level 301 so it's a real redirect, not just a JS-side one.
/fractional-cmo / 301

# Added 2026-08-25, fixing GSC page-indexing findings from this week's
# "Not found (404)" report:
# /resources/ (the bare root of the old content prefix) 404s — the splat
# rule on line 1 only matches /resources/insights/*, it never covered the
# bare root itself. Redirecting it to the closest live equivalent.
/resources/ /insights/ 301
/resources /insights 301

# when-to-hire-first-marketing-leader was removed in the same "Remove
# Fractional CMO articles" cleanup as future-fractional-cmo-operating-builder
# above (git 553aeb8) but never got a matching redirect, so it's 404'd
# since Google first indexed it back in March. Same treatment: send it to
# the closest live equivalent instead of leaving a dead end.
/insights/when-to-hire-first-marketing-leader /insights/marketing-manager-vs-fractional-cmo 301
/insights/when-to-hire-first-marketing-leader/ /insights/marketing-manager-vs-fractional-cmo 301
